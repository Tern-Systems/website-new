import { FC } from 'react';

const ScrollEnd: FC = () => <span className={'block pt-l sm:portrait:hidden sm:landscape:pt-xs'} />;
// TODO add to pages

ScrollEnd.displayName = ScrollEnd.name;

export { ScrollEnd };
