<UserControl x:Class="btmc.src.ui.forms.main.components.ComboBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Form.style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Border CornerRadius="5" Background="#808080" BorderBrush="White" BorderThickness="1">
        <Grid>
            <ComboBox x:Name="comboBox" Style="{StaticResource CustomComboBoxStyle}" 
                      ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                      GotFocus="ComboBox_GotFocus" LostFocus="ComboBox_LostFocus"/>
            <TextBlock x:Name="placeholder" Text="{Binding PlaceholderText, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="LightGray" Margin="10,0,0,0" VerticalAlignment="Center"
                       IsHitTestVisible="False" />
        </Grid>
    </Border>
</UserControl>