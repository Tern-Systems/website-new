<UserControl x:Class="btmc.src.ui.forms.main.components.ToggleNavControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:btmc.src.ui.forms.main.components"
             mc:Ignorable="d">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Form.style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    
    <Grid Margin="0,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- BTMC/TERN Toggles (centered in middle column) -->
        <Border Grid.Column="1" Width="191" Height="40" Background="Transparent" CornerRadius="20" HorizontalAlignment="Center" BorderThickness="1" BorderBrush="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <RadioButton
                    x:Name="BtmcToggle"
                    Content="BTMC"
                    FontWeight="Bold"
                    Style="{StaticResource ToggleTabStyle}"
                    Grid.Column="0"
                    Margin="0"
                    IsChecked="{Binding IsBtmcChecked, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Click="Toggle_Click" />

                <RadioButton
                    x:Name="TernToggle"
                    Content="TERN"
                    FontWeight="Bold"
                    Style="{StaticResource ToggleTabStyle}"
                    Grid.Column="1"
                    Margin="0"
                    IsChecked="{Binding IsTernChecked, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Click="Toggle_Click" />
            </Grid>
        </Border>

        <!-- Clean button (positioned in right column) -->
        <Button Grid.Column="2" Click="BtnClean_Click" Width="35" Margin="15,5" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right">
            <Image Source="/assets/icons/broom.png" Width="24" Height="24"/>
        </Button>
    </Grid>
</UserControl>