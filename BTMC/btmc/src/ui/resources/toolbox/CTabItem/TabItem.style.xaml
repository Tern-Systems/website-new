<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Rounded TabItem Template -->
    <ControlTemplate x:Key="TabItemTemplateRounded" TargetType="TabItem">
        <Border x:Name="TabBorder"
                Background="Transparent"
                CornerRadius="15"
                SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              ContentSource="Header" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabBorder"
                        Property="Background"
                        Value="{StaticResource Color.Gray.128}" />
                <Setter Property="Foreground" Value="{StaticResource Color.White}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Rounded TabItem Style -->
    <Style x:Key="RoundedTabItemStyle" TargetType="TabItem">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.GothicBold}" />
        <Setter Property="Foreground" Value="{StaticResource Color.White}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template" Value="{StaticResource TabItemTemplateRounded}" />
    </Style>

</ResourceDictionary>
