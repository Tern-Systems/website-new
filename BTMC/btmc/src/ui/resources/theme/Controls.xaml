<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- RoundedComboBox -->
  <Style x:Key="RoundedComboBox" TargetType="ComboBox">
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <Border Background="{StaticResource Brush.ToggleBackground}"
                    CornerRadius="{StaticResource Corner.Standard}"/>
            <ContentPresenter Margin="10,0,30,0"
                              VerticalAlignment="Center"
                              Content="{TemplateBinding SelectionBoxItem}"/>
            <ToggleButton x:Name="ToggleBtn"
                          IsChecked="{Binding IsDropDownOpen,
                                      RelativeSource={RelativeSource TemplatedParent}}"
                          Width="20"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalAlignment="Right">
              <Path Data="M 0 0 L 4 4 L 8 0 Z"
                    Fill="White"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </ToggleButton>
            <Popup x:Name="PART_Popup"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Placement="Bottom"
                   AllowsTransparency="True"
                   Focusable="False"
                   PopupAnimation="Slide">
              <Border Background="{StaticResource Brush.ToggleBackground}"
                      CornerRadius="{StaticResource Corner.Standard}"
                      BorderBrush="White"
                      BorderThickness="1"
                      SnapsToDevicePixels="True">
                <ScrollViewer SnapsToDevicePixels="True">
                  <ItemsPresenter/>
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="False">
              <Setter TargetName="PART_Popup"
                      Property="Visibility"
                      Value="Collapsed"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="Gray"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Pill‐style ToggleButton -->
  <Style x:Key="PillToggle" TargetType="ToggleButton">
    <Setter Property="Width"  Value="{StaticResource Toggle.Width}"/>
    <Setter Property="Height" Value="{StaticResource Toggle.Height}"/>
    <Setter Property="Background"    Value="Transparent"/>
    <Setter Property="BorderBrush"   Value="White"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Grid>
            <Border Background="Transparent"
                    CornerRadius="{StaticResource Corner.Pill}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"/>
            <Grid>
              <Border x:Name="Pill"
                      Width="{StaticResource Toggle.PillWidth}"
                      Height="{StaticResource Toggle.PillHeight}"
                      CornerRadius="{StaticResource Corner.Pill}"
                      Background="{StaticResource Brush.ToggleBackground}"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      Margin="2,0"/>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition/>
                  <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="BTMC"
                           Grid.Column="0"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="Bold"/>
                <TextBlock Text="TERN"
                           Grid.Column="1"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="Bold"/>
              </Grid>
            </Grid>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Pill"
                      Property="HorizontalAlignment"
                      Value="Left"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="False">
              <Setter TargetName="Pill"
                      Property="HorizontalAlignment"
                      Value="Right"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
